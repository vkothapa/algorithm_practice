# Count the number of set bits in a number

class Solution:
  def hammingWeight(self,n):
    count=0
    while(n):
      count=count+1
      n=n&(n-1)
    return count

# Above is a logn implementation

# Is there any O(1) implementation - like lookup
